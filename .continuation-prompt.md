Working on nexus (~/git/nexus). RDR-052 closed (PR #127). RDR-053 accepted and planned.

**Active work: RDR-053** — Xanadu Fidelity. Epic `nexus-vcyt` with 8 tasks across 2 phases.

Two tasks are ready (no blockers), parallelizable:

1. **nexus-3g5g** (P1, critical path) — Tumbler comparison operators:
   `__lt__`, `__le__`, `__gt__`, `__ge__` with -1 sentinel padding for
   cross-depth ordering. Key edge case: `1.1.3 < 1.1.3.0` must be True
   (RF-5). File: `src/nexus/catalog/tumbler.py`.

2. **nexus-tvyv** (P2) — chunk_text_hash in indexing pipeline:
   Add `chunk_text_hash = sha256(chunk_text.encode()).hexdigest()` to all
   4 indexers (code_indexer, prose_indexer, doc_indexer PDF, doc_indexer md).
   Distinct from file-level `content_hash` (RF-6). Files: `code_indexer.py`,
   `prose_indexer.py`, `doc_indexer.py`.

Critical path: `3g5g → 77wy → l8hp → 4v96 → 5arn → dx3x`

Run `bd show nexus-vcyt` for the full epic. Run `bd show nexus-3g5g` for
enriched bead descriptions with exact file paths, line numbers, and code patterns.

Key facts:
- content_hash is FILE-level, chunk_text_hash is CHUNK-level (RF-6)
- ChromaDB accepts heterogeneous metadata (RF-11, spiked)
- -1 sentinel padding passes all 10 comparison test cases (RF-12, spiked)
- Zero existing span links — no migration needed (RF-7)
- _SPAN_PATTERN must accept chash:[hex64] (gate C1)
- link_audit(t3=None) for chash verification (gate C2)

Branch: feature/nexus-zr3u.1-tumbler-hierarchy-and-templates
PR #127 open — may want to merge RDR-052 work first, then branch for 053.
