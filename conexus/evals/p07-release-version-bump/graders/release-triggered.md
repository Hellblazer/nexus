---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"release"'
---

Direct match: "bumping version, tagging, or publishing to PyPI" is the
`release` skill's own description, verbatim in shape. `release` is a
repo-local skill (`.claude/skills/release/`, not under `conexus/skills/`)
— see the README's "repo-local skills" open question.
