---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"engine-release"'
---

Direct match: "cutting or deploying the Java engine-service binary
(engine-service-vX.Y.Z), refreshing the cloud engine" is the
`engine-release` skill's own description, verbatim in shape.
`engine-release` is a repo-local skill (`.claude/skills/engine-release/`,
not under `conexus/skills/`) — see the README's "repo-local skills" open
question.
