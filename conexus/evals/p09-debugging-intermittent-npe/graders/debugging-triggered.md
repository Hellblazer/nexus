---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"(conexus:)?debugging"'
---

Direct match: "tests fail, exceptions occur, or behavior is
non-deterministic, especially after 2+ failed manual fix attempts" is the
`debugging` skill's own description — this prompt names an exception, a
non-deterministic (one-in-five) failure rate, and two prior failed
attempts.
