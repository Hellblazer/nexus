# Pending wire-contract pairings: engine halves deployed ahead of their client half

This is the ledger for the both-halves wire-contract tripwire (nexus-1vogq),
mirroring `conexus/PENDING_RELEASE.md`'s shape. It exists because of a real
incident: `498c92953` (RDR-191 GATE-2, manifest `collection` became NOT NULL
at the store) changed the engine's request validation and the client's wire
callers in the same commit. The engine tag deployed to the managed service
before any client release carried the fix. Every released client up to and
including v7.6.1 400'd on manifest writes for 34+ hours before the class was
diagnosed. Full trace: T2 `nexus/rdr-191-manifest-400-caller-trace-2026-08-14`.

`scripts/check_wire_contract_pairing.py` mechanizes detection: any commit in
`<newest published v* tag>..HEAD` that touches both the engine tree
(`service/`) and the client wire surface (client modules owning wire-contract
signatures, OR a hand-built `_post("/manifest...` / `_post("/import...` test
envelope -- the shape the 2026-08-14 bead comment added, since a raw test body
carries no method signature for a contract change to reconcile against) is a
**both-halves commit**.

**Rules, enforced by `tests/test_wire_contract_pairing_lint.py`:**

- Every both-halves commit MUST be listed under `## Unshipped` below, naming
  the owning bead and the engine tag that carries its engine half. A flagged
  commit absent from this section is UNDECLARED and fails the lint.
- When the client release carrying a commit's fix ships, move its line from
  `## Unshipped` to `## Shipped` (or delete it). An `## Unshipped` entry whose


  commit is already an ancestor of the newest published `v*` tag is STALE and
  fails the lint -- the ledger cannot quietly claim something is still pending
  once it has shipped.
- `scripts/check_engine_release_floor.py --paired-deploy` reads this ledger:
  a non-empty `## Unshipped` section blocks the paired-deploy path unless
  every entry's bead is named via `--ack-client-lag <bead-id>` (explicit
  paired-client acknowledgment, not a silent pass).
- `scripts/check_client_release_precondition.py` (protocol-audit [22511]
  Gap 1, 2026-08-14) reads this ledger too, unconditionally, on the
  UNPAIRED deploy path -- the ordinary "refresh the cloud engine" run that
  the paired-deploy branch above does not cover. Same `--ack-client-lag
  <bead-id>` escape shape.
- **Direction-safety token** (nexus-1emxn choreography (a)): an Unshipped
  entry whose note BEGINS with the literal `[additive]` asserts OLD client
  + NEW engine is safe -- the engine half may deploy BEFORE the client
  tag, so BOTH gates above treat an all-`[additive]` Unshipped section as
  authorization instead of a block (no refusal window can open for such
  entries). One shared classifier,
  `check_wire_contract_pairing.classify_unshipped`, interprets the token
  for both; before nexus-hcdk3 (2026-09-01) only the precondition gate
  honored it, and the floor gate's `--ledger-only` leg red-gated every PR
  to `main` on entries the precondition gate certified safe. `[not-additive]`
  (or NO leading token -- the fail-safe default for every pre-token entry)
  keeps the blocking behaviour. The token must LEAD the note (a
  mid-sentence mention is prose, not a statement -- the parser is anchored)
  and must be backed by the entry's own direction-safety prose naming both
  directions; `tests/test_wire_contract_pairing_lint.py` enforces the
  prose-presence half. It is an assertion of record, not a convenience.

---

## Unshipped

- `976848427` -- bead nexus-zw2em -- engine tag `TBD (next engine-service cut)` -- [additive] one NEW route, `POST /v1/vectors/upsert-reference-only` (RDR-169 G4, embed-without-store), plus the schema slice it depends on: `nexus.chunks` gains `retention TEXT NOT NULL DEFAULT 'full' CHECK (retention IN ('reference-only','full'))` and `chunk_text` becomes nullable (Liquibase `vectors-014-retention.xml`, since amended in fix round 1 with a biconditional `chunks_content_retention_consistent` CHECK -- schema-internal, no wire-shape effect). No existing route, request field or response field changed shape. FIX ROUND 1 (T2 review-nexus-zw2em-rdr169-phase-b-2026-09-11 / critique-nexus-zw2em-rdr169-phase-b-2026-09-11 / test-validation-nexus-22vvy-rdr169-phase-b-2026-09-11) added a genuine client half, recorded here by hand -- the mechanized per-commit detector cannot see this pairing, since no single commit touches both `service/` and the client wire surface. Engine half, four commits: `976848427` (this route + schema, described above); `35996c02f` (Gap 2 fix -- `plain_search_<dim>` and `text_gated_search_{hnsw_first,by_chash}_<dim>`, vectors-015, gain a `retention` output column alongside the already-nullable `content`, and `PgVectorRepository`'s two corresponding row builders now emit it -- additive, no existing field changed shape); `a383dbecd` (HTTP-boundary test only, no wire change); `a2ac4b976` (the biconditional CHECK + comment fixes, schema-internal and test/doc only, no wire change). Client half: `0b4841a36` -- `search_engine.py`'s `SearchResult` construction now coerces a reference-only row's `content: null` to `""` at the single boundary where the raw engine dict becomes a `SearchResult` (plus defense-in-depth guards in `mcp/core.py`'s render code and `formatters.py`'s CLI formatters), and `SearchResult.content`'s type widened to `str | None` to say what the wire allows. This is what makes it SAFE to expose the write route at all: before this commit, any reference-only row surfacing through plain `/v1/vectors/search` crashed the released Python client's `search`/`query` MCP tools with an opaque `TypeError`-derived error covering the WHOLE result page, not just the reference-only row -- the exact CRITICAL finding fix round 1 closes. Direction safety, both directions: OLD client (pre-`0b4841a36`) + NEW engine -- until a released client carries the None-safety fix, a reference-only row reaching plain search on an OLD client still crashes the page; no released client can currently WRITE a reference-only row (no client accessor exists for the new route, and it is reachable only by a raw authenticated HTTP caller, e.g. Conductus), so this is a live risk only once such a caller exists, not from any released nexus client's own behavior -- `[additive]` is asserted for the ROUTE'S EXISTENCE (unreachable dead surface for any client that never calls it), with the None-safety fix as the documented mitigation for the day a non-nexus caller does populate one. NEW client (`0b4841a36` or later) + OLD engine -- the None-safety guards are unconditional (they do not check engine version) and cost nothing against an engine that never returns `content: null`, so this direction is unaffected. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying `976848427`/`35996c02f`/`a2ac4b976` MUST also already carry `0b4841a36` -- the release-floor gate should refuse a bump to this engine tag without it.

- `686e3a08e` -- bead nexus-aphki -- engine tag `TBD (next engine-service cut)` -- [additive] one NEW route, `POST /v1/vectors/resolve` (RDR-169 G3, read-time URI-scheme resolution wiring): resolves a caller-supplied `source_uri` directly, or, given `collection` + `chash` instead, looks up that chunk's `metadata.source_uri` through `PgVectorRepository#get`'s existing single-id metadata read (no new SQL) and resolves THAT URI, reporting the looked-up row's own `retention` (`full`/`reference-only`, derived from whether its `chunk_text` came back null, via the `chunk_text`-nullness/`retention` CHECK biconditional the nexus-zw2em entry above landed -- no second read of the `retention` column itself). No existing route, request field or response field changed shape. Errors: 422 for an unregistered scheme, naming it and the registered set (`chroma`, `https`) -- the CLIENT-side schemes (`file://`, `obsidian://`, `x-devonthink-item://`, `nx-scratch://`) are deliberately never registered here, since a managed engine cannot reach a tenant's local machine (RDR-169's split-by-reachability design); 404 for an unknown chash or a chash whose metadata carries no `source_uri`; 502 for a resolver-side failure (a dangling `https://` URL, a missing `chroma://` row), carrying the resolver's own error reason/detail, never a stack trace; 503 for the `(collection, chash)` form when no pgvector repository is wired. Recorded here by hand -- the mechanized per-commit detector cannot see this pairing, since the engine commit and the client commit are separate. Engine half: `686e3a08e` (`UriSchemeResolverRegistry` constructed and wired live in `NexusService`'s constructor -- `HttpsSchemeHandler` registered for `https` unconditionally and closed in `stop()`, `ChromaSchemeHandler` registered for `chroma` whenever a `PgVectorRepository` is wired -- plus the new `ResolveHandler` at a more specific HTTP context than `/v1/vectors`). Client half: `173648243` -- `HttpVectorClient.resolve_content(source_uri=..., collection=..., chash=...)`, propagating the engine's 404/422/502/503 as `VectorServiceError` with `.code` set, the client's existing 4xx/5xx handling shape. Direction safety, both directions: OLD client + NEW engine -- no released client calls the new route, so it is unreachable dead surface for an old client, and `com.sun.net.httpserver`'s longest-prefix-match routing dispatches `/v1/vectors/resolve` to the new context before `VectorHandler`'s own switch ever sees the path, so every existing `/v1/vectors/*` route stays byte-identical. NEW client + OLD engine -- an engine older than this tag has no `/v1/vectors/resolve` context registered and answers a stock non-JSON 404, which `_post`'s existing `HTTPError` handling already surfaces as `VectorServiceError(code=404)` -- fails loud, never a silent no-op or a fabricated result. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying `686e3a08e`.

## Shipped

- `36ab3504d` -- bead nexus-em75s.14 -- shipped in `v7.41.0` -- engine half engine-service-v0.1.114 (tagged on a2801dfc9; deployed and live BEFORE the v7.41.0 client tag, 2026-09-11) -- [additive] one NEW route family, `/v1/tuples` (RDR-205: `out`, `rd`, `rdp`, `in`, `inp`, `ack`, `nack`, `registry`, `subspace_list`, `subspace_stats`), with its own tables (`nexus.tuples`, `nexus.tuple_claim_log`, `nexus.tuple_tenants`; changesets tuples-001-baseline.xml and tuples-002-sweep-indexes.xml, seven changesets) and a second scheduled sweep task. No existing route, request field or response field changed shape. Engine half: the fourteen `service/` commits cf1e5d7b0..a2801dfc9 (Phase 1 and its follow-ons). Client half: `HttpTupleStore` and `db.tuples` (77f9fdfa8), the eight `tuple_*` MCP tools, `nx tuple` and three doctor rows (d635c60be, 41bc87f2d), the two async projection hooks (1d147d591). Direction safety, both directions: OLD client (7.40.0) + NEW engine -- no released client calls any `/v1/tuples` route or reads the new tables, so the route family is unreachable dead surface for an old client, and every existing route is byte-identical; the new sweep task touches only the new tables. NEW client + OLD engine (v0.1.113) -- every tuple call receives the engine's 404, which the client surfaces as `httpx.HTTPStatusError` (fails loud) rather than one of `HttpTupleStore`'s typed errors, since the old engine never registers the `/v1/tuples` route at all and returns a stock non-JSON 404 that `_raise_typed`'s fallback re-raises untyped; the three doctor rows stay informational below the tuple route's first-shipping tag (`_TUPLE_ROUTE_FIRST_ENGINE_VERSION`, bumped to this tag in the paired client release together with `REQUIRED_ENGINE_VERSION`); the async projection hooks skip and log on any non-2xx and never hold a dispatch. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to this tag.

- `d8c9077d1` -- bead nexus-c0g6e -- shipped in `v7.39.0` -- engine half engine-service-v0.1.113 (tagged on e222a8fb6; deployed and live BEFORE the v7.39.0 client tag, 2026-09-10) -- [additive] two NEW routes: `GET /v1/taxonomy/topics/doc_count_drift` (read-only, no params, returns `[{topic_id, label, collection, doc_count, actual_count}, ...]` for every topic this tenant whose cached doc_count disagrees with its real topic_assignments count) and `POST /v1/taxonomy/topics/recount_doc_count` (body `{"dry_run"?: bool}`, absent/non-bool = `false`; applies -- or, `dry_run`, previews -- the correction tenant-wide, returns `{dry_run, corrected, topics: [...]}` with the same row shape). Both call a shared `nexus.*` SQL function (`taxonomy-016-doc-count-drift-functions.xml`) also called by `hygiene-007-1`'s boot walk. No existing route, request field or response field changed shape. Client half (`3416b7ebd`): `HttpTaxonomyStore.get_doc_count_drift`/`recount_doc_count`, the `nx doctor` "topics.doc_count drift" row (now one batched call instead of a per-topic loop), and `nx taxonomy audit --fix-doc-count`. Direction safety, both directions: OLD client + NEW engine -- no released client calls either route, so they are unreachable dead surface for an old client; every other taxonomy route is byte-identical. NEW client + OLD engine -- the doctor check and the audit flag both read 404 as "engine does not report this route yet" (an explicit skip/unread state, never a false all-clear), never a fallback to the retired per-topic loop. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying these routes.
- `14c97916a` -- bead nexus-ft04v.16 -- shipped in `v7.38.0` -- engine half engine-service-v0.1.112 (tagged on 9f0a5397c; deployed and live BEFORE the v7.38.0 client tag, 2026-09-09) -- [additive] no route, request field or response field changes shape. Engine half: `PgVectorRepository.restoreRereferenced` resolves the quarantine sibling's dimension from the ORIGIN collection's row again (as shipped engine-service-v0.1.109 does), not from the sibling's own row, which Phase 2 (`fa49ee418`) had made it read: the sibling has no row until something is quarantined, so the first server-side GC pass over any collection answered 422 and the client's broad except logged `gc_serverside_prune_failed` and stopped (seven such lines in the v0.1.110 shakeout). Client half (`src/nexus/indexer.py`): the pre-registration of the quarantine sibling added in the Phase 3 fix round (`c1e870c18`, which had masked the 422 by minting an empty sibling row and left three orphan projection rows in the v0.1.111 shakeout's Phase E) is deleted; `quarantine_registration_kwargs` and its tests go with it. Direction safety, both directions: OLD client (7.37.0) + NEW engine -- the first GC pass now succeeds where it 422'd against v0.1.110 (never deployed), byte-identical to its behaviour against v0.1.109; NEW client + OLD engine (v0.1.109) -- that engine already resolves from the origin, and the deleted pre-registration was never something an old engine required. Ack condition: none required -- the wire shape is unchanged and the pairing is the same Phase 2 cut this ledger already names.
- `586c7e3d1` -- bead nexus-ft04v.33 -- shipped in `v7.38.0` -- engine half engine-service-v0.1.112 (tagged on 9f0a5397c; deployed and live BEFORE the v7.38.0 client tag, 2026-09-09) -- [additive] one NEW read-only route, `GET /v1/catalog/embedding_profile`, returning the calling tenant's `nexus.embedding_profile` rows as `{"profile": [{"content_type", "embedding_model", "dimension"}, ...], "count": N}`, ordered by content_type, RLS-scoped through the `nexus.tenant` GUC (an unprofiled tenant gets `count: 0` and an empty list, never a default). Engine half (this commit): `CatalogRepository.embeddingProfile`, `CatalogHandler.handleEmbeddingProfile` and its dispatch case, the envelope-gate entry; RDR-204 Phase 2 item 4 (bead nexus-ft04v.33, engine half only). Client half: NONE in this commit -- the `HttpCatalogClient` accessor is Phase 3 work (nexus-ft04v.33's client half, blocking .26 and .31) and is deliberately absent. No existing route, request field or response field changed shape. Direction safety, both directions: OLD client + NEW engine -- no released client calls the route, so it is unreachable dead surface for an old client; every other catalog route is byte-identical, no behaviour change. NEW client + OLD engine -- there is no new client code in this commit; when the Phase 3 accessor lands it must FAIL LOUD on the engine's 404 (`{"error":"not found: /embedding_profile"}`) rather than fall back to a hardcoded model (the GH #667 class), which is that bead's own acceptance test. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this route AND whose Phase 3 accessor reads it.
- `a70971b00` -- bead nexus-ft04v.24 -- shipped in `v7.38.0` -- engine half engine-service-v0.1.112 (tagged on 9f0a5397c; deployed and live BEFORE the v7.38.0 client tag, 2026-09-09) -- [additive] two read routes grow, nothing changes shape. `GET /v1/vectors/stats` rows gain `content_type`, `owner_id`, `embedding_model`, `lifecycle_state` (LEFT JOIN on `nexus.catalog_collections`; the four keys are OMITTED, not null, for a collection with vector stats and no catalog row, the same convention `last_write` uses; the population is unchanged). `GET /v1/catalog/collections/list` gains optional `content_type` and `lifecycle_state` query parameters (blank or absent = unfiltered, byte-identical to today) and each row gains `dimension` and `lifecycle_state`. Client half: none in this commit; bead nexus-ft04v.26 (Phase 3) reads the new keys and sends the filters. Direction safety, both directions: NEW client + OLD engine -- no released client sends the parameters or reads the keys yet, and the Phase 3 client is required to fail loud on the missing filter (RDR-204 §Cross-Cutting), never to fan out over unfiltered rows; OLD client + NEW engine -- an unfiltered list call returns the same rows with two extra keys, the stats rows carry four extra keys, and every existing key is unchanged. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this commit.
- `c7f2ecda2` -- bead nexus-ft04v.16 -- shipped in `v7.38.0` -- engine half engine-service-v0.1.112 (tagged on 9f0a5397c; deployed and live BEFORE the v7.38.0 client tag, 2026-09-09) -- [additive] `POST /v1/vectors/embed` (the embed-only parity/comparison route, bead nexus-gmiaf.21 -- it never stores anything) gains an optional `model` field naming an embedding-model token directly, as an alternative to `collection`: exactly one of the two is now required (400 naming both fields if neither or both are present). `collection` keeps its existing registry-resolved routing (`EmbedderRouter.resolveEmbedderStrict`, unchanged since bead nexus-ft04v.16's own landing) byte-for-byte -- same 422s, same message shapes. `model` is a wholly NEW code path (`EmbedderRouter.resolveEmbedderByModel`), dispatching directly by model token with no collection lookup at all; 422 (`EmbeddingModelUnavailableException`) naming the token and the current embedding mode when the mode has no embedder for it. No existing route, request field or response field changed shape; the 200 response envelope (`{"embeddings": [...]}`) is identical for both request shapes. Client half: NONE in this commit -- no released client, and no client code anywhere in this repo, posts to `/v1/vectors/embed` at all (it is a Java-test/shell-probe-only route: `EmbedParityTest`, `tests/db/test_embed_parity.py`, `service/native-smoke.sh`, `service/linux-native-verify.sh`, `service/trace-native.sh`, `Bge768ServiceEmbedIntegrationTest`, all updated in this commit to send `model` instead of an unregistered `collection`). Direction safety, both directions: OLD client + NEW engine -- there is no client caller of this route at all, so nothing can observe the new field; NEW client + OLD engine -- symmetric, no client code sends `model` today, and none is planned (the route stays a test/probe-only parity gate, not a production client accessor). Ack condition: none required -- there is no client half to pair against, ever, for this route.
- `0c97fc06e` -- bead nexus-ft04v.16 -- shipped in `v7.38.0` -- engine half engine-service-v0.1.112 (tagged on 9f0a5397c; deployed and live BEFORE the v7.38.0 client tag, 2026-09-09) -- [not-additive] the read-side behaviour change bead nexus-ft04v.16's own landing (`fa49ee418`) made to EVERY vector read route, plus its fan-out mitigation (`c7f2ecda2`, substantive-critic C1) and this commit's caller-visibility channel, together: a read addressed at a single collection with no `catalog_collections` row (`dimForCollection`, `searchTopicScopedWithTokens`, every write path) now answers 422 (`UnregisteredCollectionException`) where the pre-`fa49ee418` name-parse engine answered an empty result instead. This part of the change is unaffected by anything below. A multi-collection FAN-OUT read (`searchWithTokens`, `hybridSearchWithTokens`, `searchMetadataScopedWithTokens`, `searchAspectScopedWithTokens`, `searchGraphHopWithTokens`) does not inherit that abort: `c7f2ecda2`'s `registeredSurvivors` DROPS an unregistered name from the fan-out instead, and this commit reports every dropped name back to the caller as a response HEADER, `X-Nexus-Skipped-Collections` (comma-separated names), set only when non-empty -- 422 only when NONE of the requested names survive. The response BODY never changes shape for this: it stays the pre-existing bare-array (or, with `rerank=true`, the pre-existing rerank-envelope) response in every case, skip or no skip -- a HEADER an old client's parser has no code path to inspect is, by construction, inert to it, which is why this is a header and not a body field (the first pass of this commit put it in the body, unwrapping only when non-empty; that was reverted before landing once the shipped 7.37.0 client's `http_vector_client.py` was checked and found to unwrap a body object envelope ONLY when `rerank=true`, meaning a body-level field would have broken every non-rerank caller in exactly the stale-cache-race window this mechanism exists to degrade gracefully through -- worse than the log-only version it replaced). Direction safety, both directions: OLD client (7.37.0) + NEW engine -- a direct read of a just-deleted or never-registered collection (the client's collection cache is fed by `/v1/vectors/stats`, ~60s TTL, so a stale name is reachable for up to a minute) gets a 422 the client renders as an error instead of the old empty-result response (this half is unchanged from the fan-out-only mitigation, still tracked here for direction-safety completeness); a fan-out over the same stale name loses nothing (the survivors still come back) and the response body the old client parses is byte-identical to today -- the header naming the drop is present on the wire but the old client's parser never looks for it, so it is silently ignored, exactly as inert as the drop already was under the pre-`c7f2ecda2` server-only log, with no new failure mode introduced. NEW client + OLD engine -- the header is never present (an old engine predates both `registeredSurvivors` and this header and still aborts the whole fan-out on one unregistered name), unchanged from today. Ack condition: the client release paired with the Phase 2 engine cut, whose `REQUIRED_ENGINE_VERSION` bump carries this tag AND whose Phase 3 helper repoint (bead nexus-ft04v.16's client half) reads the `X-Nexus-Skipped-Collections` header and renders the single-collection 422 as an actionable error rather than an empty result.
- `38b5bc4fce1c2c950f05ae267d10b75c35cffbd4` -- bead nexus-ft04v.8 -- shipped in `v7.37.0` -- engine half engine-service-v0.1.109 (tagged on 2839915eb; deploy AFTER the v7.37.0 client tag, non-additive pairing) -- [not-additive] `POST /v1/catalog/collections/upsert` now returns HTTP 422 when a NEW registration's `embedding_model` disagrees with the tenant's install-scoped `nexus.embedding_profile` row for that content_type (or, for an EXISTING row, disagrees with that row's own model) -- RDR-204 Technical Design step 2 (bead nexus-ft04v.8). Engine half (this commit): `CatalogRepository.upsertCollection`'s profile-driven model resolution + new `EmbeddingProfileConflictException`, and `CatalogHandler.handleCollectionUpsert`'s dispatch catch chain (422 JSON mapping). Client half: bead nexus-ft04v.34, ALREADY ON ORIGIN as `8aaf417f1`, plus bead nexus-f5wwx (`b2b91ee2d12d26e63755479e533cda72cd3519aa`, `e299237e80381f9e822bf53bc5ec5c385af8ea93`: the client's store, aspects, taxonomy, manifest and rename paths register a collection before the first write, since this engine no longer auto-registers on a chunk write) -- `commands/index.py` stops hardcoding `voyage-context-3` and instead sends `effective_embedding_model_for_writes` (the tenant's own resolved model), landed ahead of this engine half specifically so a client upgraded past `8aaf417f1` never disagrees with its own tenant's profile. Direction safety: NEW client (nexus-ft04v.34 or later) + NEW engine (this commit) -- the client always sends the profile's own model, so `upsertCollection`'s comparison never disagrees, no 422; OLD client (pre-`8aaf417f1`, still hardcoding `voyage-context-3` for every CCE-bucket content type) + NEW engine (this commit) -- an ONNX-mode/local install's profile is `bge-base-en-v15-768`, not `voyage-context-3`, so that old client's `nx index repo` collection registration starts getting 422s it did not get before -- the regression this NOT-additive classification exists to flag; NEW client + OLD engine (pre-this-commit) -- the engine has no profile-driven resolution yet and accepts whatever model is sent, unchanged from today. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this commit, confirmed to also already carry nexus-ft04v.34 (`8aaf417f1`, already on develop as of this writing).
(none)

- `c78be96ad8b68bb55a3be0069403c4e632578261` -- bead nexus-zewg3 -- shipped in `v7.36.0` -- engine half engine-service-v0.1.108 (tagged on fa21c24ed; deployed 2026-09-08T01:13Z; STEP-6 PASSED gate-report-20260908T011402Z-v011.json) -- [additive] `GET /v1/catalog/manifest/chashes` gains an OPT-IN, additive `tombstone_protected_count` field -- computed ONLY when the request carries `with_tombstone_protected=1`/`true` -- reporting how many of the returned chashes are held alive ONLY by a pending tombstone (never a live document), tenant-wide, per `CatalogRepository.tombstoneProtectedChunkCount`. `nx t3 gc`'s report line reads this instead of re-deriving it client-side. Engine half (this commit): `CatalogHandler.handleManifestChashes`, `CatalogRepository.tombstoneProtectedChunkCount`. Client half: `392778bdffdce7f428852d7b94347ca479224401` (`HttpCatalogClient.chashes_for_collection_with_tombstone_protected`, `nx t3 gc`'s report line), landed as a SEPARATE commit -- the mechanized per-commit detector (`scripts/check_wire_contract_pairing.py`) cannot see this pairing, since neither commit alone touches both `service/` and the client wire surface in the same commit; this entry records the risk by hand (code review T2 `nexus/critique-nexus-zewg3-engine-side` Significant 2). No existing request or response FIELD changed shape, only added to, and only when explicitly requested. Direction safety, both directions: OLD client + NEW engine -- `chashes_for_collection` (the indexer's hot per-collection call, `nexus.indexer._prune_deleted_files`, invoked on every `nx index repo` run) never passes `with_tombstone_protected`, so the field is never computed or returned to a caller that doesn't ask for it; an old client that doesn't know the key simply never requests or reads it, no behavior change, no added query cost. NEW client + OLD engine -- an engine older than this commit ignores the unrecognized `with_tombstone_protected` query param (same envelope as before) and never emits the key, so `chashes_for_collection_with_tombstone_protected`'s `"tombstone_protected_count" in result` check is False and the second return value is `None`, never a fabricated `0`; `nx t3 gc` reports "Protected by pending tombstones: unavailable on this engine" (`test_gc_reports_tombstone_protected_as_unavailable_on_an_older_engine`). Client half shipped in conexus 7.36.0 (REQUIRED_ENGINE_VERSION (0,1,108)).
- `6e94e5b544ee416fbd76f57b5e458bd703d39fdb` -- bead nexus-8hdg9 -- shipped in `v7.33.0` -- engine half engine-service-v0.1.105 (tagged on 01639ae88; deployed 2026-09-06T19:54Z after conexus's PITR-fork walk rehearsal; STEP-6 PASSED gate-report-20260906T195544Z-v011.json, recorded in deployed-engine-version 20:05Z) -- [additive] phases 3/4 observability: `GET /v1/status`'s two activity shapes GROW by one field, `deadline_aborts_total` (a monotonic per-embedder count of embed calls aborted at a cooperative request-deadline check point -- `Bge768Embedder.embedSubBatched` between ONNX sub-batches, `CceEmbedder.embedParallel` before each collected future -- raising `RequestDeadlineExceededException`, the 503 + `Retry-After` the `e146905867a6` entry below declared). Present in every `embedder_activity` entry and in `local_embed_activity` when that is non-null (`StatusHandler.appendSnapshot`, from `EmbedActivitySnapshot.deadlineAbortsTotal`). This is the A/B gate's instrument: a healthy indexing run must read 0. The same commit family makes the phase-2/5 deadline REACHABLE (the check points now exist) and adds the `NX_EMBED_DEADLINE_MAX_MS` hard ceiling (default 900000 ms) that clamps both the `X-Nexus-Request-Deadline-Ms` header budget and the `NX_EMBED_DEADLINE_MS` default; neither is a wire-shape change (the header and the 503 were already declared below), only the status field is. Direction safety, both directions: OLD CLIENT + NEW ENGINE -- the new field is present in the body but `http_engine_status.py` reads only the keys it knows (`active`, `chunks_done_total`, `last_activity_age_ms`, ...) and ignores unknown ones, so nothing on an old client changes behavior; the newly reachable 503 lands on the client's existing `_GATEWAY_RETRY_CODES` ladder exactly as the `e146905867a6` entry describes. NEW CLIENT + OLD ENGINE -- no client code in this commit family reads `deadline_aborts_total` yet (the A/B gate reads it from the container's engine directly, not through the client), so a new client against an old engine that lacks the field sees nothing different; an old engine also never raises the 503, so the client path is byte-identical to today. No existing request or response FIELD changed shape, only added to. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this commit.

- `41d4aab7e` -- bead nexus-8hdg9 -- shipped in `v7.33.0` -- engine half engine-service-v0.1.105 (tagged on 01639ae88; deployed 2026-09-06T19:54Z after conexus's PITR-fork walk rehearsal; STEP-6 PASSED gate-report-20260906T195544Z-v011.json, recorded in deployed-engine-version 20:05Z) -- [additive] phase 5: the client declares its own embed budget on `POST /v1/vectors/upsert-chunks` via a NEW advisory request header, `X-Nexus-Request-Deadline-Ms` (client half: `http_vector_client._request_once` stamps `_UPSERT_CHUNKS_DEADLINE_MS` = socket timeout minus a 60s margin = 540000, route-keyed on the `/upsert-chunks` suffix only, never on the search family; the margin is pinned by `tests/test_embed_deadline_default_ordering.py`), and the engine uses it IN PLACE OF the `NX_EMBED_DEADLINE_MS` default when minting `RequestContext.deadlineNanos()` (engine half: `RequestDeadline.resolveBudgetMs` called from `AuthFilter.doFilter` -- a present, positive, numeric header wins outright, larger than the env default included, since the design's reason for the header is that a server-guessed deadline kills healthy slow requests the client would still wait for; the env default is the fallback only; a malformed or non-positive header is IGNORED, never a 400, because the header is advisory). Still UNREACHABLE as a 503 in practice: no embed-loop check point reads the deadline yet (phases 3/4, each gated on its own timed A/B). Direction safety, both directions: OLD CLIENT + NEW ENGINE -- the header is absent, `resolveBudgetMs(null, envDefault)` returns the env default, the deadline minted is byte-identical to phase 2's, no behavior change (proven by `AuthFilterTest.absentHeaderFallsBackToEnvDefault`); NEW CLIENT + OLD ENGINE -- `X-Nexus-Request-Deadline-Ms` is an unknown request header, which `com.sun.net.httpserver` ignores, so the request is handled exactly as today, no behavior change. No existing request or response FIELD changed shape, only a request header added. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this commit.

- `e146905867a682d167100150d3c9b4c3d9d5d263` -- bead nexus-8hdg9 -- shipped in `v7.33.0` -- engine half engine-service-v0.1.105 (tagged on 01639ae88; deployed 2026-09-06T19:54Z after conexus's PITR-fork walk rehearsal; STEP-6 PASSED gate-report-20260906T195544Z-v011.json, recorded in deployed-engine-version 20:05Z) -- [additive] `RequestDeadlineExceededException`'s HTTP mapping (any embed-bearing `/v1/vectors/*` route) gains a `Retry-After` header + `retry_after_seconds` body field alongside its existing 503 (`VectorHandler`'s catch arm, mirroring `UpstreamRateLimitedException`'s 429 arm shape exactly). UNREACHABLE in this commit -- no embed-loop check point raises this exception yet (phases 3/4, nexus-8hdg9.3/.4, each gated on its own timed A/B before landing); declared here now, ahead of reachability, so the pairing check does not flag the engine half again in isolation once phases 3/4 land and this commit is already an ancestor. Direction safety, both directions: OLD CLIENT + NEW ENGINE -- `http_vector_client.py`'s existing `_GATEWAY_RETRY_CODES` ladder (`_once_with_gateway_retry`) already retries any 502/503/504 unconditionally on its own fixed 2.0/5.0/10.0s backoff schedule, so an old client handles this specific 503 exactly like every other gateway-transient code today -- no crash, no new failure mode, no behavior change. The new `Retry-After`/`retry_after_seconds` are present on the wire but an old client does NOT read them on this path (verified: the generic gateway-retry loop parses no response header today; only the UNRELATED rerank envelope's `rerank_retry_after_seconds` structured field feeds the client rate brake) -- so this commit does not yet make the client honour anything new, it only makes the DATA available for a future client update to consume; NEW CLIENT + OLD ENGINE -- there is no new client-side code in this commit (its only Python touch, `_UPSERT_CHUNKS_TIMEOUT_S`, is a renamed constant with the SAME value 600 replacing a bare literal, not a behavior change), so a build carrying this commit talking to an old engine sees nothing different either. No existing request or response FIELD changed shape, only added to. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this commit AND whose client half actually reads `Retry-After` on the vector gateway-retry path -- expected to land alongside phases 3/4, when the exception becomes reachable and there is a real reason for the client to consume the header.

- `db5dbfceb481bd4ebbe4f46fdaedcf371dde0f3e` -- bead nexus-gjv9b -- shipped in `v7.33.0` -- engine half engine-service-v0.1.105 (tagged on 01639ae88; deployed 2026-09-06T19:54Z after conexus's PITR-fork walk rehearsal; STEP-6 PASSED gate-report-20260906T195544Z-v011.json, recorded in deployed-engine-version 20:05Z) -- [additive] PART 3 prerequisite (bead note 2026-09-05, critique-nexus-gjv9b-16b72394f Significant 5): `nexus.capability_census` gains a NEW nullable JSONB column, `capabilities_by_scope` (telemetry-013-capability-census-scope-split.xml), holding `{"orchestrator": {cap: count, ...}, "subagent": {cap: count, ...}}` over the same 8-value CAPABILITIES vocabulary the flat `cap_*` columns already stamp -- NULL on a blindspot row or a pre-split writer, never a fabricated all-zero breakdown. Engine half (this commit): `TelemetryRepository.recordCapabilityCensus` gains a `capabilitiesByScopeJson` param (jOOQ `CAPABILITY_CENSUS.CAPABILITIES_BY_SCOPE` via the existing `JsonbSupport.jsonbOrNull` helper, same pattern as `hook_failures.batch_doc_ids`); `queryCapabilityCensus` parses the column back into a nested object (Jackson, mirroring `CatalogRepository`'s own jsonb-to-object convention for `metadata`) rather than the raw-string convention `batch_doc_ids` uses, since this field's whole point is structural readback. `POST /v1/telemetry/capability_census/record` accepts two OPTIONAL new body fields, `capabilities_orchestrator`/`capabilities_subagent` (same 8-value vocabulary as the existing `capabilities` field), which the handler combines server-side into the one stored column; the existing `capabilities` field (the merged total) is unchanged. `GET /v1/telemetry/capability_census/query`'s response rows gain an OPTIONAL `capabilities_by_scope` key (`null` when the split was never sent for that row). Client half in the same commit: `HttpTelemetryStore.record_capability_census` gains matching `capabilities_orchestrator`/`capabilities_subagent` kwargs (omitted from the wire payload when `None`); `_session_end_census.build_capability_census_record` computes the split from the session's existing `CorpusCensus` result (`orchestrator_calls`/`subagent_calls`, already used for the merged total) and threads it through `write_session_capability_census`/`_post_capability_census`; `nx census capability --from-store` renders `capabilities_by_scope` when a row carries it (JSON: passed through verbatim; text: an added `by_scope` line per session). Direction safety, both directions: NEW client + OLD engine -- the two new request fields are silently ignored by an old handler (unrecognized body keys), so the split is measured client-side but never persisted or visible via `--from-store` until the engine tag deploys (never a crash, never a wrong merged total -- `capabilities` is sent and stored exactly as before); the `GET .../query` response simply has no `capabilities_by_scope` key, and the CLI's `row.get("capabilities_by_scope")` truthiness check treats that identically to an explicit `null` (no extra line, no KeyError); OLD client + NEW engine -- the column and the two new request fields exist but nothing on an old client sends or reads them, so every row's `capabilities_by_scope` stays NULL and the response shape old callers already expect (a `capabilities` dict, no split key) is unaffected -- `dict.get` on the client side never asserts the key's presence. CONFIRMED BY TEST, not by reading (coordinator directive, critique-nexus-gjv9b-part3-db5dbfceb fold-in, commit 078f1c223): a later same-feature commit added a flat/scope consistency invariant to `handleCapabilityCensusRecord` (400 when a caller's flat `capabilities` count disagrees with `capabilities_orchestrator + capabilities_subagent`) -- this invariant is gated behind `capabilities_orchestrator`/`capabilities_subagent` both being ABSENT (empty maps), so the exact wire body every conexus 7.31.0-and-earlier client sends (a full flat `capabilities` map, neither split field present at all) is accepted 200 with `capabilities_by_scope` staying NULL, never a 400 and never an absent split being treated as an implicit all-zero split. Verified by `CapabilityCensusAndRoutingEventsHandlerTest.census_record_old731ClientShape_acceptedWithNullScope` (real engine, testcontainers) and `test_http_telemetry_store.py::TestRecordCapabilityCensus.test_old_client_shape_omits_scope_split_keys_entirely` (confirms the client's own payload omits both keys entirely, not as empty objects, when the split kwargs are not passed). No existing request or response FIELD changed shape, only added to. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this column and these two routes' new fields.

- `2b255517017eac28c1ab857f8f047a7bff5d8fbb` -- bead nexus-dt2tu.2 -- shipped in `v7.33.0` -- engine half engine-service-v0.1.105 (tagged on 01639ae88; deployed 2026-09-06T19:54Z after conexus's PITR-fork walk rehearsal; STEP-6 PASSED gate-report-20260906T195544Z-v011.json, recorded in deployed-engine-version 20:05Z) -- [additive] one NEW route, `POST /v1/telemetry/nx_answer_runs/complete`, plus one NEW `/version` field, `nx_answer_run_complete_supported`. Engine half in this commit: `TelemetryRepository.recordNxAnswerRunComplete` (one `withTenant` transaction over `nx_answer_runs`, `nx_answer_steps` and `plans`, reusing the lifted `insertRunAndSteps` and the new package-private static `PlanRepository.incrementRunStartedIn` / `incrementRunOutcomeIn`), `TelemetryHandler.handleNxAnswerRunComplete`, and the `VersionHandler` flag. The new route REQUIRES `outcome` and `created_at`, 400 on either missing or unparsable; `created_at` is the dedup key that makes a retried composite idempotent. That requirement is scoped to `/complete` alone: `POST /v1/telemetry/nx_answer_runs/record` keeps its lenient handling (absent `created_at` stamps `now()`) unchanged, which the ETL path and three surviving client writers still rely on. No existing route, request field or response field changed shape. No DDL: the three tables already exist. Direction safety, both directions: OLD client + NEW engine -- the route exists and the flag is present in `/version`, but no released client posts to `/complete` or reads the flag, so every old client keeps issuing `run_start`, `nx_answer_runs/record` and `run_outcome` exactly as before and observes no change; NEW client + OLD engine -- the capability probe reads a `/version` body with no `nx_answer_run_complete_supported` key, degrades to the same three calls, and a 404 from `/complete` (a downgrade under a running process) flips the cached flag false and falls back for the rest of the process, so the record is never lost. A7: the public conexus edge trims `/version` to a reviewed allowlist, so `nx_answer_run_complete_supported` is invisible to cloud clients until conexus allowlists it (the `nx_answer_steps_supported` / 7.14.0 and nexus-bwulw precedent) -- a cloud-mode probe reading "unsupported" is the edge, not an engine defect, and the relay is owned by P4 (nexus-dt2tu.4). Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this route AND whose client half posts to `/complete` behind the probe.
- `44127004554437f3c6b6cdcfe1ffbc3c303105cb` -- bead nexus-nukn3 -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] fold-in on the `/index_failures/acks` route below: it now accepts an OPTIONAL `?file_path=` query param (engine half in this commit: `TelemetryRepository.listIndexFailureAcknowledgments(tenant, filePath)` overload, `TelemetryHandler.handleIndexFailureAcksList` reads it, both delegating to the existing no-arg form with a blank filter), the remedy for the code-review's finding that `--unacknowledge --file`'s auto-resolve was paging every acknowledgment tenant-wide and filtering client-side -- the same class of fix `?file_path=` on `/index_failures/list` already got. Client half in this commit: `HttpTelemetryStore.list_index_failure_acknowledgments` gains a `file_path` kwarg; `nx index failures --unacknowledge --file` calls it server-side-filtered. Direction safety, both directions: NEW client + OLD engine -- an old engine ignores the unrecognized `?file_path=` query param entirely (unrecognized query params are simply dropped, not rejected), so the auto-resolve gets every acknowledgment back unfiltered and re-applies its OWN client-side ambiguity check against the full set -- a multi-file backlog can misresolve to "ambiguous, pass --error-class explicitly" where a filtered engine would have resolved cleanly, but never a crash or a wrong revoke (the ambiguity guard fails safe); OLD client + NEW engine -- the query param exists but nothing on an old client sends it, no behavior change. No existing request or response FIELD changed shape, only added to. Ack condition: same as the index_failures entries below (one client release, one engine tag, carries all seven routes together).

- `be444581c47f0491ec741d76fdf649df90175b71` -- bead nexus-4jj40 -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] no new route or field: the combined-write path (CombinedWriteService, the ChunkBatcher flush's mechanism) now refreshes chunk METADATA for chashes it skips as already present (PgVectorRepository.batchUpdateMetadata), so a client `nx index repo --force` without `--re-embed` re-stamps section_type on unchanged chunks at zero embed cost. Direction safety, both directions: NEW client + OLD engine -- the request shape is unchanged; the old engine skips the metadata refresh as before, so a forced reclassify pass is a silent no-op on existing chunks (no error, no data loss; `--re-embed` still works as the expensive lever); OLD client + NEW engine -- the refresh only touches metadata the client already sends, no behaviour change visible to an old client. Ack condition: the client release whose REQUIRED_ENGINE_VERSION bumps to the engine tag carrying this change.

- `50cc6dfff43770664096ba157fbada77ad43e6c5` -- bead nexus-nukn3 -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] third fold-in on the index_failures entries below: two NEW routes, `GET /v1/telemetry/index_failures/acks` and `POST /v1/telemetry/index_failures/unacknowledge` (engine half `3c76b6b627a856bc0ad540d969a746d9c036d3a6`: TelemetryRepository.listIndexFailureAcknowledgments + revokeIndexFailureAcknowledgment, both scoped to `kind='acknowledgment'` and disjoint from trimIndexFailures' `kind='failure'` reaper; `getIndexFailures` gains a 6-arg overload with a server-side exact `filePath` filter, the 5-arg overload unchanged and delegating with `filePath=""`), the remedy for the critique's finding that acknowledgment rows were write-only -- an operator adjudication that could never be listed or revoked. Client half in this commit: `HttpTelemetryStore.list_index_failure_acknowledgments` / `unacknowledge_index_failure`, plus `file_path` on `list_index_failures`; `nx index failures --acks` (list) and `--unacknowledge (--file PATH | --error-class CLASS)` (revoke, exact-match, mirroring the creation scope) are new flags, pairwise mutually exclusive with `--clear`/`--acknowledge`; the `--acknowledge --file` auto-resolve now calls the new server-side `file_path` filter instead of paging 1000 rows tenant-wide; `nx doctor --check-index-failures` gains an informational footnote naming the active-acknowledgment count. Direction safety, both directions: NEW client + OLD engine -- `/acks` and `/unacknowledge` 404, caught by the same try/except as the other index_failures routes, `--acks`/`--unacknowledge` raise a clean ClickException naming "route not found on this engine"; the `--acknowledge --file` auto-resolve falls back to `list_index_failures(limit=1)` against an old engine that does not understand `file_path` as a query param -- the param is simply ignored server-side, returning the tenant's single most-recent failure regardless of file, which can misresolve the error_class for a multi-file backlog until the engine tag deploys (never a crash); OLD client + NEW engine -- the two new routes and the `filePath` query param exist but nothing on an old client calls or sends them, no behavior change. No existing request or response FIELD changed shape, only added to. Ack condition: same as the index_failures entries below (one client release, one engine tag, carries all seven routes together).

- `4d5520bf4f5e7c6a15bfcc2b091730bafa78b0c3` -- bead nexus-nukn3 -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] second fold-in on the index_failures entries below: a fifth route, `POST /v1/telemetry/index_failures/acknowledge` (engine half `694a923c8336357f420654acfd10c83bc1656f67`: TelemetryRepository.recordIndexFailureAcknowledgment + a new `kind` column on the existing table, telemetry-012-index-failures-kind.xml -- CHECK-constrained, DEFAULT 'failure' so every historical row backfills correctly with no data migration; `getIndexFailures` gains an `unacknowledgedOnly` param + a per-row `acknowledged` boolean, both via a correlated NOT EXISTS/EXISTS subquery against `kind='acknowledgment'` rows; `trimIndexFailures` gains `dry_run` and is now always scoped to `kind='failure'`), the durable-adjudication remedy for the critic's Critical finding that the latest-run-within-30-days doctor gate never self-heals for a permanently unextractable file re-indexed on a cadence shorter than that window (every run mints a fresh run_id for the identical failure). Client half in this commit: `HttpTelemetryStore.acknowledge_index_failure`; `nx index failures --acknowledge` (new flag); `nx doctor --check-index-failures`'s gate now queries `unacknowledged_only=True`. Direction safety, both directions: NEW client + OLD engine -- the acknowledge route 404s, caught by the same try/except as the other four index_failures routes, `nx index failures --acknowledge` raises a clean ClickException naming "route not found on this engine"; the OLD engine's `list_index_failures`/`getIndexFailures` response simply has no `acknowledged` key and ignores an unrecognized `?unacknowledged_only=` query param (its 4-arg signature never changed on the wire -- the 5th param is engine-side only, addressed via the query string, not a positional shift), so the doctor gate silently falls back to gating on EVERY failure (the round-1 behavior, not round-2's improvement, but never a crash or a false-clean read) until the engine tag deploys; OLD client + NEW engine -- the route and the `kind` column exist but nothing on an old client calls or reads them, no behavior change. No existing request or response FIELD changed shape, only added to. Ack condition: same as the index_failures entries below (one client release, one engine tag, carries all five routes together).

- `410720f6ac83aeef6bec1a20a161a813338be8a5` -- bead nexus-nukn3 -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] three new routes: `POST /v1/telemetry/index_failures/record`, `POST /v1/telemetry/index_failures/record_batch`, `GET /v1/telemetry/index_failures/list` (engine half `2f98abdfee0a4575c6cffc2261ab635efc8b9081`: TelemetryHandler.java, backed by a new `nexus.index_failures` table, telemetry-009-index-failures.xml) -- a durable per-file failure record for the repo-index path, replacing a log line + in-memory counter. Client half in this commit: `HttpTelemetryStore.record_index_failure` / `record_index_failures_batch` / `list_index_failures`; `nexus.indexer._run_index` calls the batch-record + list-read pair advisorially at end-of-run (wrapped in a broad try/except that falls back to the in-memory skip count on any failure); `nx index failures` (new CLI verb) and `nx doctor --check-index-failures` (new supplementary check, in the default sweep) both call `list_index_failures`. Direction safety, both directions: NEW client + OLD engine -- all three routes 404; the indexer's try/except catches the HTTP error and falls back to the in-memory skip count (the pre-nukn3 behavior, unchanged), `nx index failures` and the doctor check surface an explicit "service backend unreachable" / UNKNOWN message rather than a crash or a false-clean 0; OLD client + NEW engine -- the routes exist but nothing on an old client calls them, no behavior change (the failure record and read surface are simply unavailable until the client tag ships, same as any other brand-new instrument). No existing request or response shape changed, only added to. Ack condition: the client release whose REQUIRED_ENGINE_VERSION bumps to the engine tag carrying this route.

- `37262c4a1f7a6d8315e304dcaa03c418bc210f0e` -- bead nexus-nukn3 -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] fold-in on the index_failures entry above: a fourth route, `POST /v1/telemetry/index_failures/trim` (engine half `67bbd0eb5003eefa5ef4510e45ab2fd25d89d5db`: TelemetryRepository.trimIndexFailures + TelemetryHandler.handleIndexFailureTrim, 400s when both `run_id` and `days` are absent; no new table/column, deletes from the existing `nexus.index_failures`), the remedy for the critic's Critical finding that an all-time fail-first doctor check needs a way back to green. Client half in this commit: `HttpTelemetryStore.trim_index_failures`; `nx index failures --clear` (new flag on the existing verb) calls it; `nx doctor --check-index-failures`'s message text now points operators at `--clear`. Direction safety, both directions: NEW client + OLD engine -- the trim route 404s, caught by the same try/except as the other three index_failures routes, `nx index failures --clear` raises a clean ClickException naming "route not found on this engine" rather than a crash or a silent no-op; OLD client + NEW engine -- the route exists but nothing on an old client calls it, no behavior change. No existing request or response shape changed, only added to. Ack condition: same as the index_failures entry above (one client release, one engine tag, carries all four routes together).

- `e83210bb30c7d2b12c7b67d839a44a7eb417513b` -- bead nexus-gjv9b -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] two NEW tables + four NEW routes, replacing two JSONL logs (Sam directive 2026-08-20). Engine half (this commit): `nexus.capability_census` (telemetry-010-capability-census.xml, one row per (tenant_id, session_id), UPSERT) + `POST/GET /v1/telemetry/capability_census/{record,query}`; `nexus.routing_events` (telemetry-011-routing-events.xml, append-only event log) + `POST /v1/telemetry/routing_events/{record,batch}` + `GET .../list` (engine half for routing_events also lands in this commit's shared `TelemetryRepository`/`TelemetryHandler` files, even though the routing-hook WRITER swap itself is the following commit, `16fd7f07409e7f71aefc59dab8a339983d4b9617`). Rebased over nexus-nukn3's `telemetry-009-index-failures.xml`, which claimed the `telemetry-009` slot first -- this bead's two changesets renumbered from `telemetry-009`/`telemetry-010` to `telemetry-010`/`telemetry-011` accordingly (SHAs in this entry are the rebased ones). Client half in this commit: `HttpTelemetryStore.record_capability_census`/`query_capability_census`/`record_routing_event`/`list_routing_events` (all new methods, no existing method touched). Direction safety, both directions: NEW client + OLD engine -- every new route 404s; `record_capability_census`/`record_routing_event` are both used ONLY from single-attempt, hard-timeout best-effort call sites (`_session_end_census._post_capability_census`, the routing hooks' own `urllib` POST) that already treat ANY failure — a 404 included — as the cue to metered-drop, never a crash or a blocking retry; `query_capability_census`/`list_routing_events` raise, and their only callers (`nx census capability --from-store`, `nx hook routing-stats --from-store`) catch and report `UNAVAILABLE: <reason>` with a non-zero exit, never a crash. OLD client + NEW engine -- the new routes exist but nothing on an old client calls them, no behavior change. No existing request or response shape changed, only added to. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying these routes.

- `16b72394ff0666b911881d0bfe84892bd1e16855` -- bead nexus-gjv9b -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] review fold-in on the `e83210bb3` entry above (critique Significant 4): two NEW routes, `POST /v1/telemetry/capability_census/trim` and `POST /v1/telemetry/routing_events/trim` (engine half this commit: `TelemetryRepository.trimCapabilityCensus`/`trimRoutingEvents`, same age-only `{days, dry_run}` body contract as `hook_failures/trim` -- no new table/column, deletes from the two existing tables the `e83210bb3` entry created). Client half in this commit: `HttpTelemetryStore.trim_capability_census`/`trim_routing_events`; `nx doctor --trim-telemetry` now sweeps these two tables alongside `search_telemetry`/`hook_failures` under the same `--dry-run` contract. Direction safety, both directions: NEW client + OLD engine -- both trim routes 404, caught by `_run_trim_telemetry`'s existing `httpx.HTTPError`/`RuntimeError` handling (reported as `Error: telemetry trim unavailable`, never a crash or a silent no-op that reports a completed sweep it did not do); OLD client + NEW engine -- the routes exist but nothing on an old client calls them, no behavior change. No existing request or response shape changed, only added to. Ack condition: same as the `e83210bb3` entry above (one client release, one engine tag, carries all six routes together).

- `4a0cb2ce4124bcbccdc48e1c5fdfc3dd43ed174b` -- bead nexus-s71lr -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] `GET /v1/status`'s shape GROWS (pass 3, T2 [24547]) beyond the `c4bb1a9de` entry below: adds `"embedder_activity": {"<model_token>": {"active", "chunks_done_total", "sub_batches_total", "last_chunks_per_sec", "last_activity_age_ms", "queue_depth", "thread_width"}, ...}`. `local_embed_activity` (the `c4bb1a9de` field, UNCHANGED) stays `null` in cloud/voyage mode or when no local admission gate is wired. `embedder_activity` (NEW field, this commit) is a map keyed by each embedder's own `modelToken()` (`EmbedderRouter.embedActivitySnapshots()`, generic over `Embedder.activitySnapshot()` — a new default-null interface method VoyageEmbedder/CceEmbedder override, Bge768Embedder already did) — covers cloud mode's voyage-code-3/voyage-context-3/voyage-3 (the majority posture `local_embed_activity` alone never reported), plus local mode's bge768 redundantly for uniformity. `queue_depth`/`thread_width` stay -1 for cloud embedders (no `LocalOnnxAdmission`-equivalent there). Client half in the same commit: `src/nexus/db/http_engine_status.py`'s `format_engine_activity_line()` falls back to `embedder_activity` (picking the freshest/most-recently-active entry) when `local_embed_activity` is null, still wired into `nx doctor --check-engine-activity`. Direction safety, both directions: NEW client + OLD engine -- `embedder_activity` key absent from an old body, `.get("embedder_activity") or {}` degrades to the existing "no embedder activity reported" message, never a crash; OLD client + NEW engine -- the new field is present but nothing on an older client reads it, no behavior change (a client that only reads `local_embed_activity` is unaffected -- that field's shape is untouched). No existing request or response field changed, only added to. Ack condition: same as `c4bb1a9de` below.

- `c4bb1a9debe2ffee3e312e6cd999cfb44a93c53a` -- bead nexus-s71lr -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] new `GET /v1/status` route (StatusHandler.java) serving live embed-activity counters: `{"embedding_mode": "...", "local_embed_activity": null | {"active", "chunks_done_total", "sub_batches_total", "last_chunks_per_sec", "last_activity_age_ms", "queue_depth", "thread_width"}}`. `local_embed_activity` is `null` in cloud/voyage mode or when no local admission gate is wired -- never fabricated. Unauthenticated, same posture as `/health`/`/version` (operational telemetry, no trust boundary crossed). Client half in the same commit: new `src/nexus/db/http_engine_status.py` (`fetch_engine_status()` / `format_engine_activity_line()`, fail-closed -- never raises, `None` on any transport error or missing route), wired into `nx doctor --check-engine-activity` (also promoted into the default sweep). Direction safety, both directions: NEW client + OLD engine -- `GET /v1/status` 404s, `fetch_engine_status()` catches it and returns `None`, `nx doctor` renders "Engine activity: UNKNOWN (status endpoint unreachable — pre-nexus-s71lr engine, or service down)", never a crash (proven in `tests/test_http_engine_status.py::test_fetch_engine_status_returns_none_on_404_pre_s71lr_engine`); OLD client + NEW engine -- the route exists but nothing on the old client calls it, no behavior change. No existing request or response shape changed. Ack condition: the client release whose `REQUIRED_ENGINE_VERSION` bumps to the engine tag carrying this route.

- `940cb5a0aeb0e2d6de88ab32cb68b3614828bcb7` -- bead nexus-n75jg -- shipped in `v7.32.0` -- engine half engine-service-v0.1.104 (tagged on 7a89ab2ea; deployed and STEP-6 gate PASSED per T2 deployed-engine-version, recorded 2026-09-05T23:21Z, before the client tag) -- [additive] rerank degrade envelope gains a structured `rerank_retry_after_seconds` field (RerankStage.java), populated only for an UpstreamRateLimitedException-caused degrade (1vpal critic finding 2), so the client rate brake (conexus-cy9u7) can pace on rerank-driven throttling instead of parsing rerank_error's free text. Client half in the same commit: HttpVectorClient.search's rerank_meta_out gains retry_after_seconds (None when absent) and feeds it to nexus.rate_brake.get_brake().trip(source="rerank") when present. Direction safety, both directions: NEW client + OLD engine -- the new field is never in the envelope, retry_after_seconds stays None, brake never trips on this signal (unchanged behavior, the gap this bead exists to close persists until the engine deploys); OLD client + NEW engine -- the client ignores the unknown rerank_retry_after_seconds key entirely (unchanged behavior). No existing request or response shape changed. Ack condition: the client release whose REQUIRED_ENGINE_VERSION bumps to the engine tag carrying this field.
- `b319141c8` -- bead nexus-ndoke -- shipped in `v7.28.0` -- engine half engine-service-v0.1.96 (tagged 2026-09-03, deployed and STEP-6 green before the client tag; telemetry-008 cache-token columns), client half in the same commit; [additive] both directions, deployed ahead of the client tag per nexus-1emxn (a).

- `033f8d71b` -- bead nexus-vi8fp -- shipped in `v7.27.0` -- engine half engine-service-v0.1.94 (tagged 2026-09-02 on 13c5b2c31, alongside the 1vpal/spbay payloads; pre-tag battery green; ZERO Liquibase changesets in the delta so no PITR fork walk; all-[additive], so the engine deployed and cloud-gated GREEN before the client tag per nexus-1emxn choreography (a)) -- [additive] opt-in any-lexeme plan-search fallback. Engine half: `PlanRepository.searchPlans` 5-arg `anyLexeme` overload (4-arg stays AND-only, byte-identical for every existing caller) + `POST /v1/plans/search` OPTIONAL `any_lexeme` body field (absent = unchanged wire). Client half in the same commit: `HttpPlanLibrary.search_plans(any_lexeme=False)` keyword, field OMITTED from the payload unless True; only the `plan_search` MCP tool passes True; `plan_match` never does (decision C on the T2 [23891] admission measurement — auto-admission ran junk plans on 9/10 unrelated probes). Direction safety, both directions: NEW client + OLD engine = the unknown body field is ignored by the old handler, AND-only results, exactly today's behavior (the browsing surface just gains nothing until the tag deploys); OLD client + NEW engine = the field is never sent, the 4-arg-equivalent path runs, byte-identical results. No existing request or response shape changed.

- `e24a91c4a` -- bead nexus-spbay -- shipped in `v7.27.0` -- engine half engine-service-v0.1.94 (tagged 2026-09-02 on 13c5b2c31; that tag also carries the gated nexus-4ktfm + nexus-99r7y payload and nexus-1vpal; deployed and cloud-gated GREEN before the client tag, all-[additive]) -- [additive] telemetry `since` filter parsing. Engine half: `TelemetryRepository.parseSinceFilter` (date-only -> midnight UTC, naive datetime -> UTC, malformed -> IllegalArgumentException -> the handler's 400) at the three since-filter sites (`GET /v1/telemetry/nx_answer_runs/query`, `/tier_writes/query`, `/tier_writes/list`) — replacing the write-stamp `parseTs`, whose malformed-input fallback silently turned any unparseable `since` into `since now()` (a guaranteed empty set). Client half in the same commit: `normalize_since_filter` in `http_telemetry_store.py` converts date-only/naive forms to full UTC ISO BEFORE the wire, plus CLI pre-try validation. Direction safety, both directions: NEW client + OLD engine = the client only ever sends full offset ISO (the one form the old parser reads correctly), so the fix is complete client-side against the deployed engine — verified live 2026-08-31 (`--since 2026-08-01` returns 42 rows on the v0.1.93 cloud). OLD client + NEW engine = a well-formed `since` parses identically; a malformed one now gets an explicit 400 instead of a silently-empty 200 — every since-bearing caller wraps the read in a broad except with an honest failure message, so the change surfaces as a loud degrade, never a crash (fail-loud in the direction this repo's doctrine requires).

- `5ac9f09e03e554d34acd17b6062cfbc16a6212d9` -- bead nexus-ogccs -- shipped in `v7.25.0` -- engine half engine-service-v0.1.92 (tagged 2026-08-31 on 14bd80442; pre-tag battery green — service-ci full suite, CANDIDATE SHAKEOUT PASSED incl. the native-smoke probe set, published-client write gate PASSED on the 7.24.1 client; ZERO Liquibase changesets in the delta so no PITR fork walk; all-[additive], so the deploy fires BEFORE the client tag per nexus-1emxn choreography (a)) -- [additive] ONNX model root resolution (env/HOME, never bare passwd user.home). Engine half: OnnxModelPaths (NX_ONNX_MODEL_DIR -> $HOME/.cache/nexus/onnx_models -> user.home) feeds Bge768Embedder + CrossEncoderReranker DEFAULT_*_PATH, boot log event=onnx_model_root. Client half in the same commit: nexus.db.onnx_model_root shared root, provisioner dirs derive from it, supervisor pins NX_ONNX_MODEL_DIR in the engine spawn env. Not a wire-shape change (env-var contract, not HTTP): NEW client + OLD engine = env var ignored, pre-fix behaviour; NEW engine + OLD client = HOME rung alone fixes supervisor spawns. Ack condition met: 7.25.0 bumps REQUIRED_ENGINE_VERSION to (0,1,92), and the plugin-cut rehearsal Dockerfile's passwd-home==sandbox-HOME alignment comes out in the same commit as the bump.

- `b9ab656061182af607086e6cc2276825534d3637` -- bead nexus-zu9ln -- shipped in `v7.24.0` -- engine half engine-service-v0.1.91 (tagged 2026-08-30 on 762b9d7ba; v0.1.90 burned on a stale native-smoke.sh probe; pre-tag battery green, PITR fork-walk CLEAN with every NOTICE count matching, deploy fires at the v7.24.0 client-tag push per the paired choreography) (next cut from develop tip; PITR-fork walk rehearsal required, the changeset is cumulative) hygiene-001-not-null: `POST /v1/aspects/upsert` and `POST /v1/aspects/queue/enqueue` / `enqueue_many` now return 400 on a blank `doc_id` (engine half: AspectHandler + AspectRepository; schema: document_aspects.doc_id/source_uri and aspect_extraction_queue.doc_id NOT NULL); `POST /v1/plans/save` returns 409 on a NULL verb (plans.verb NOT NULL). Client half in the same commit: `aspect_extraction_enqueue_hook` resolves a blank doc_id through the catalog and skips loudly instead of enqueueing ""; `HttpDocumentAspectsStore.upsert` and `HttpPlanLibrary.save_plan` raise before the wire. Direction safety: NEW client + OLD engine = the client never sends a blank doc_id/verb, unchanged wire otherwise; OLD client + NEW engine = a pre-7.24 client that still enqueues store_put rows with doc_id="" gets a 400 it logs as a best-effort warning (aspect extraction for that ingest is skipped, never a crash), and its `nx enrich aspects-without-catalog` exits 2 on the retired route. NOT additive: the deploy must be ARMED before the client tag push (nexus-1emxn), and the client release bumps REQUIRED_ENGINE_VERSION to (0,1,91) (7.24.0).
- `b9ab656061182af607086e6cc2276825534d3637` -- bead nexus-zu9ln -- shipped in `v7.24.0` -- engine half engine-service-v0.1.91 (tagged 2026-08-30 on 762b9d7ba; v0.1.90 burned on a stale native-smoke.sh probe; pre-tag battery green, PITR fork-walk CLEAN with every NOTICE count matching, deploy fires at the v7.24.0 client-tag push per the paired choreography) route REMOVED both halves in one commit: `GET /v1/aspects/list_without_catalog_document` (engine: AspectRepository.listWithoutCatalogDocument + AspectHandler) and its client `HttpDocumentAspectsStore.list_without_catalog_document` + `nx enrich aspects-without-catalog` (+ `--sweep`). With document_aspects.doc_id NOT NULL under the fk-001 cascade an orphan aspect row cannot exist, so the census had nothing left to count. OLD client + NEW engine = the verb exits 2 naming the missing route (nexus-mlu3k's designed behaviour); NEW client + OLD engine = no caller.
- `f34a4b82f20d28b0aee14c4f3dc1062d6d7c6351` -- bead nexus-fgxmk -- shipped in `v7.23.0` -- engine half engine-service-v0.1.89 (tagged 2026-08-29 on 55be868db; pre-tag battery green, deploy fires at the v7.23.0 client-tag push per the paired choreography) -- `GET /v1/catalog/search` gains `ORDER BY tumbler` (engine half: CatalogRepository.searchDocuments) and the client documents that order contract + gains `find_by_title_exact` (client-only). Direction safety: NEW client + OLD engine = results still unordered (the docstring's order promise is unmet, nothing breaks; `find_by_title_exact` filters client-side and needs no engine change); OLD client + NEW engine = deterministic order where there was none. No request shape changed.
- `d0291180f2cbb098140f9833c54c3d652dcef10c` -- bead nexus-mlu3k -- shipped in `v7.23.0` -- engine half engine-service-v0.1.89 (tagged 2026-08-29 on 55be868db; pre-tag battery green, deploy fires at the v7.23.0 client-tag push per the paired choreography) -- NEW read-only route `GET /v1/aspects/list_without_catalog_document` (engine half: AspectRepository.listWithoutCatalogDocument + AspectHandler) and its client `HttpDocumentAspectsStore.list_without_catalog_document` + `nx enrich aspects-without-catalog`. Direction safety: NEW client + OLD engine = the route 404s and the verb exits 2 naming the missing route (never an empty census); OLD client + NEW engine = an extra route nothing calls. Additive, no existing request shape changed.
- `98911306253b3cb4dfe22fdaf8a5e730d5cfc92b` -- bead nexus-fgxmk -- shipped in `v7.23.0` -- engine half engine-service-v0.1.89 (tagged 2026-08-29 on 55be868db; pre-tag battery green, deploy fires at the v7.23.0 client-tag push per the paired choreography) -- javadoc + docstring wording only (string order, not registration order); no wire behaviour beyond the line above.
- `bc1847e08` -- bead nexus-lqqb2 -- shipped in `v7.22.0` -- engine half engine-service-v0.1.88 (tagged 2026-08-28 on 2ca52773f; pre-tag battery + PITR fork-walk CLEAN, deploy fires at the v7.22.0 client-tag push per the paired choreography) -- dead consent-audit wire DELETED both halves in one commit (client `record_consent`/`list_consents` + engine `/v1/telemetry/consents/{record,list}`). Deletion, so no released client calls it; an old client against a new engine gets 404 on a route nothing invokes.
- `72d25594e` -- bead nexus-v0x32 -- shipped in `v7.22.0` -- engine half engine-service-v0.1.88 (tagged 2026-08-28 on 2ca52773f; pre-tag battery + PITR fork-walk CLEAN, deploy fires at the v7.22.0 client-tag push per the paired choreography) -- `nx telemetry baseline`'s `GET /v1/telemetry/relevance/stats`; the client renders UNAVAILABLE-with-reason on an engine that lacks the route (fail-loud by design, no wedge).
- `36fad4487` -- bead nexus-8tnz2 -- shipped in `v7.22.0` -- engine half engine-service-v0.1.88 (tagged 2026-08-28 on 2ca52773f; pre-tag battery + PITR fork-walk CLEAN, deploy fires at the v7.22.0 client-tag push per the paired choreography) -- `GET /v1/catalog/docs/collection-counts-all` (all-rows counts for the drop-orphan-collections arm); an old engine 404s and the arm reports INCOMPLETE, refusing --execute (designed for exactly this window; extension pass a228e079).
- `c2d9c4302` -- bead nexus-ubnwk -- shipped in `v7.22.0` -- engine half engine-service-v0.1.88 (tagged 2026-08-28 on 2ca52773f; pre-tag battery + PITR fork-walk CLEAN, deploy fires at the v7.22.0 client-tag push per the paired choreography) -- `POST /v1/vectors/search-aspect-scoped` + `nexus.search_aspect_scoped_<dim>` (vectors-008) + aspects-004 doc_id backfill; the MCP tool fails loud through `_mcp_tool_error` on 404 until the tag deploys.

- `cc61d4c31` -- bead nexus-nyry9.10 -- shipped in `v7.14.0` -- engine half engine-service-v0.1.85 (deployed + cloud-gated 2026-08-21, paired client release) -- RDR-196 Phase 1: `steps[]` on POST /v1/telemetry/nx_answer_runs/record (engine half e87d2d9c7, nexus-nyry9.9: telemetry-007-1/-2 nx_answer_steps table + RLS, /version `nx_answer_steps_supported`), `include_steps` on the query route + nullable `nx_answer_runs.cost_usd` (engine half 93c3d7bf3, nexus-lme1s: telemetry-007-3), client half cc61d4c31 (steps write-through behind a /version capability probe; run cost_usd = sum of steps or null). Direction safety: new engine + old client = unchanged wire (steps optional, include_steps default off, null cost tolerated); old engine + new client = probe reports unsupported, run row only, never a 400. Shipped: REQUIRED_ENGINE_VERSION bumped to (0,1,85) for conexus 7.14.0; engine tagged on 73b6d4a0c, --acquire gate PASSED 2026-08-21.
- `6a7ff9915` -- bead nexus-tk070.p6b -- shipped in `v7.13.0` -- engine half engine-service-v0.1.84 (deployed + cloud-gated 2026-08-20 BEFORE the cut); RDR-194 D5, TWO INDEPENDENT wire surfaces, analyzed separately: (a) frecency SQL CHECK + boundary-400 -- genuinely new engine behavior, needs pairing. Engine half: nexus.frecency/staging.frecency ttl_days 0->NULL rewrite + CHECK (frecency_ttl_days_positive_chk, telemetry-006-frecency-ttl-null.xml) + TelemetryHandler.requirePositiveOrNullTtlDays boundary-400 on POST /v1/telemetry/frecency/upsert. Client half: T3Database.put/HttpVectorClient.put/nx store put/nx memory promote reject an explicit ttl_days<=0 with a typed ValueError BEFORE any wire call (nexus-24rof). Pairing, both directions: NEW client + OLD engine -- client-side rejection fires pre-HTTP, an un-paired old engine never sees a 0 from this client; safe. OLD client + NEW engine -- a pre-this-bead client can still send ttl_days=0; the new engine's CHECK/boundary-400/409 rejects it loudly, the intended D5 fail-loud, not a regression. Ack condition: the client release whose REQUIRED_ENGINE_VERSION bumps to the RDR-194 P7 paired engine tag carrying this CHECK. (b) The T3-chunk-metadata predicate flip (HttpVectorClient.expire $ne:0 -> $gt:0) needs NO pairing: safe specifically because $gt numeric-operand support was already live server-side before this bead (nexus-4l80g) -- a property of this operator's deployment history, not a general client-predicate truth.
- `f2d979113` -- bead nexus-tk070.p6a -- shipped in `v7.13.0` -- engine half engine-service-v0.1.84 (deployed + cloud-gated 2026-08-20 BEFORE the cut); RDR-194 D5: nexus.memory/nexus.plans ttl_days CHECK constraints (memory-003-ttl-days.xml, plans-003-ttl-days.xml) + MemoryHandler.requirePositiveOrNullTtl boundary-400 (engine half); MCP core.py ttl coercion deletion (client half). Ack condition: the client release whose REQUIRED_ENGINE_VERSION bumps to the RDR-194 P7 paired engine tag carrying these CHECKs. Bounded blast radius in the interim: an engine predating this commit still carries the retired coercePermanentTtl, so a client already carrying core.py's change talking to an un-paired old engine gets the pre-existing silent-coerce-to-NULL behavior, unchanged from before this bead.

- `f1c669b4792b43dc15d48dce344c48fa695bf287` -- bead nexus-zu4ma -- shipped in `v7.11.0` -- Bge768 padded-token-area sub-batching (engine half: engine-service-v0.1.81, deployed + cloud-gated 2026-08-19); client half (http_telemetry_store.py) rides v7.11.0, whose REQUIRED_ENGINE_VERSION=(0,1,82) floor carries the (0,1,81) bump (nexus-5uoxu conditions 2+4)

- `3b2901141627c98a4ad1c182bf021b44703d6d33` -- bead nexus-o8dil.33 -- shipped in `v7.8.0` -- catalog-030 (engine half: engine-service-v0.1.79); client half shipped 2026-08-17 in the paired release; the forced check was performed pre-deploy: v7.7.0's only callers were doctor diagnostics (degradation acknowledged via --ack-client-lag during the paired window, resolved the moment v7.8.0 published)

- `498c92953ea3ad60a75389aea53a9f501d8b126a` -- bead nexus-sh9v2 -- shipped in `v7.7.0` -- RDR-191 GATE-2: caller-supplied manifest collection, NOT NULL at the store (engine half: engine-service-v0.1.73)
- `b361a8106953c0bb586ab3aac969f904d3dff9df` -- bead nexus-rnqbw -- shipped in `v7.7.0` -- one protection semantics for the delete paths; manifest rows carry their own collection on the wire (engine half: engine-service-v0.1.74)
- `8c75a61a3fd1d65f61695263ea1b0961377c358d` -- bead nexus-sh9v2 -- shipped in `v7.7.0` -- F8c manifest guard passes the rows' own collection; rebuilt the GATE-2 tripwire (engine half: engine-service-v0.1.75 test coverage, no main-code touch)
