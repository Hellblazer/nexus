[project]
name = "conexus-mcpb"
version = "5.1.5"
description = "Conexus packaged as Claude Desktop .mcpb (Desktop Extension)"
requires-python = ">=3.12"
dependencies = [
    "conexus>=5.1.5",
]

[tool.uv]
package = false
