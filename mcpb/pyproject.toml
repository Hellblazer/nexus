[project]
name = "conexus-mcpb"
version = "5.1.1"
description = "Conexus packaged as Claude Desktop .mcpb (Desktop Extension)"
requires-python = ">=3.12"
dependencies = [
    "conexus>=5.1.1",
]

[tool.uv]
package = false
