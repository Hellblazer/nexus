[project]
name = "conexus-mcpb"
version = "5.4.2"
description = "Conexus packaged as Claude Desktop .mcpb (Desktop Extension)"
requires-python = ">=3.12"
dependencies = [
    "conexus>=5.4.2",
]

[tool.uv]
package = false
