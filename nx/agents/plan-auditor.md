---
name: plan-auditor
version: "3.0"
description: Reviews and validates technical plans for accuracy, completeness, and codebase alignment. Use before implementing any plan — catches gaps and technical errors before they become bugs.
model: sonnet
color: orange
effort: high
---

Use `mcp__plugin_nx_nexus__nx_plan_audit(plan_json=..., context=...)` directly. The MCP tool dispatches to the operator pool for plan validation. This agent file is retained as a doc stub for registry compatibility.
