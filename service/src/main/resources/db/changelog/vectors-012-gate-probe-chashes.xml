<?xml version="1.0" encoding="UTF-8"?>
<!--
    nexus-zrcj7 (T2 nexus/critique-zrcj7-step1-hybrid-typing-and-double-recheck-2026-09-04
    [24219], finding B: "single gate evaluation" lost) — vectors-011's text_gate_probe_<dim>
    returned only a COUNT, discarding the matched chashes; the selective branch
    (text_gated_search_<dim>, vectors-010) then independently re-evaluated the FULL text
    gate (including the expensive `<%` trigram heap-recheck) a SECOND time to materialize +
    rank. The retired Java hybridSearch ran the gate's expensive `<%` recheck exactly ONCE
    (nexus-x7z7l, conexus-qsa): its single bounded probe fetch returned the actual gate
    chashes, and the selective rank reused them via a cheap chash-IN PK lookup, never
    re-running the text gate. This changeset restores that: text_gate_probe_<dim> becomes a
    SET-returning function of the gate chashes (still capped at p_cap, Postgres cannot
    ALTER a function's return type via CREATE OR REPLACE, hence the DROP+CREATE below), and
    a new text_gated_search_by_chash_<dim> ranks ONLY over that chash set plus scope
    (collection + tombstone + where) — no text gate re-evaluation at all.

    nexus.text_gate_probe_384/768/1024(p_text text, p_collections text[], p_where jsonb,
    p_where_path text, p_cap int) RETURNS SETOF bytea — byte-for-byte the SAME gate
    predicate as vectors-011's original (COUNT-returning) version, now projecting the
    matched chash instead of discarding it. A SETOF-of-a-scalar-type function becomes a
    jOOQ generated Table (single unnamed column), the same table-valued dispatch idiom
    plain_search_<dim>/text_gated_search_<dim> already use — NOT the Routines scalar
    dispatch the retired COUNT version used, since the return shape changed from scalar to
    set. Java counts the fetched rows for the selective-vs-dense dispatch decision (byte-
    equivalent to the retired code's gateChashes.size() comparison against
    SELECTIVE_GATE_MAX) and, for the selective case, passes the exact chash array forward.

    nexus.text_gated_search_by_chash_384/768/1024(p_query vector, p_chashes bytea[],
    p_collections text[], p_where jsonb, p_where_path text, p_n int) RETURNS TABLE(id text,
    content text, collection text, distance float8, metadata jsonb) — ranks by cosine
    distance over `chash = ANY(p_chashes)` plus the SAME scope predicates every sibling
    rank function carries (collection membership, tombstone dead-set, where
    containment/jsonpath, embedding IS NOT NULL) — deliberately NO text-gate predicate
    (`chunk_tsv @@ ...` / `<% `): gate membership was already proven by the probe fetch
    above, so re-checking it here would be exactly the double trigram-recheck this
    changeset exists to eliminate. The tombstone/where re-check IS a harmless repeat of
    what the probe already verified (byte-for-byte matching the retired code's own
    behavior — its chash-IN rank query re-applied the SAME scope filter the probe used,
    never skipped it), unlike the text gate which is genuinely NOT re-applied.

    hybridSearch's selective branch now calls text_gated_search_by_chash_<dim> instead of
    text_gated_search_<dim> (vectors-010's function stays defined, unchanged, and still
    directly exercised by PlainSearchTextGatedSearchExplainTest's own EXPLAIN/oracle tests
    against its row_number()-over-materialized-gate shape — it is simply no longer the
    function hybridSearch's Java dispatch calls for the selective path).

    LANGUAGE sql, STABLE, SECURITY INVOKER, NOT STRICT (the default) — same discipline as
    every sibling combined-query function; text_gate_probe_<dim> inlining is irrelevant to
    HNSW reachability (it never touches embedding/HNSW at all), but LANGUAGE sql keeps it
    consistent with every other function in this family. EXECUTE defaults to PUBLIC
    (vectors-007 precedent) — no explicit GRANT EXECUTE here.

    Placed after vectors-011-hybrid-selectivity-dispatch.xml (current tail) and before the
    grants-nexus-svc.xml runAlways block — the established late-append convention (D0.1).
-->
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="vectors-012-1" author="nexus-zrcj7">
        <comment>text_gate_probe_384/768/1024: SETOF bytea (gate chashes, capped at p_cap) replacing the COUNT-only version -- restores single-gate-eval, nexus-zrcj7</comment>
        <sql splitStatements="true" stripComments="false">
DROP FUNCTION IF EXISTS nexus.text_gate_probe_384(text, text[], jsonb, text, int);
DROP FUNCTION IF EXISTS nexus.text_gate_probe_768(text, text[], jsonb, text, int);
DROP FUNCTION IF EXISTS nexus.text_gate_probe_1024(text, text[], jsonb, text, int);
        </sql>
        <sql splitStatements="false" stripComments="false">
CREATE FUNCTION nexus.text_gate_probe_384(
    p_text         text,
    p_collections  text[],
    p_where        jsonb,
    p_where_path   text,
    p_cap          int
) RETURNS SETOF bytea
LANGUAGE sql
STABLE
SECURITY INVOKER
AS $$
    SELECT c.chash
      FROM nexus.chunks c
     WHERE c.collection = ANY(p_collections)
       AND (NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m
                         JOIN nexus.catalog_documents d
                           ON d.tenant_id = m.tenant_id AND d.tumbler = m.doc_id
                        WHERE m.tenant_id = c.tenant_id AND m.chash = c.chash
                          AND d.deleted_at IS NOT NULL
                          AND NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m2
                                            JOIN nexus.catalog_documents d2
                                              ON d2.tenant_id = m2.tenant_id AND d2.tumbler = m2.doc_id
                                           WHERE m2.tenant_id = m.tenant_id AND m2.chash = m.chash
                                             AND d2.deleted_at IS NULL)))
       AND (p_where IS NULL OR c.metadata @&gt; p_where)
       AND (p_where_path IS NULL OR c.metadata @@ p_where_path::jsonpath)
       AND (c.chunk_tsv @@ plainto_tsquery('english', p_text) OR p_text &lt;% c.chunk_text)
     LIMIT p_cap
$$;

CREATE FUNCTION nexus.text_gate_probe_768(
    p_text         text,
    p_collections  text[],
    p_where        jsonb,
    p_where_path   text,
    p_cap          int
) RETURNS SETOF bytea
LANGUAGE sql
STABLE
SECURITY INVOKER
AS $$
    SELECT c.chash
      FROM nexus.chunks c
     WHERE c.collection = ANY(p_collections)
       AND (NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m
                         JOIN nexus.catalog_documents d
                           ON d.tenant_id = m.tenant_id AND d.tumbler = m.doc_id
                        WHERE m.tenant_id = c.tenant_id AND m.chash = c.chash
                          AND d.deleted_at IS NOT NULL
                          AND NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m2
                                            JOIN nexus.catalog_documents d2
                                              ON d2.tenant_id = m2.tenant_id AND d2.tumbler = m2.doc_id
                                           WHERE m2.tenant_id = m.tenant_id AND m2.chash = m.chash
                                             AND d2.deleted_at IS NULL)))
       AND (p_where IS NULL OR c.metadata @&gt; p_where)
       AND (p_where_path IS NULL OR c.metadata @@ p_where_path::jsonpath)
       AND (c.chunk_tsv @@ plainto_tsquery('english', p_text) OR p_text &lt;% c.chunk_text)
     LIMIT p_cap
$$;

CREATE FUNCTION nexus.text_gate_probe_1024(
    p_text         text,
    p_collections  text[],
    p_where        jsonb,
    p_where_path   text,
    p_cap          int
) RETURNS SETOF bytea
LANGUAGE sql
STABLE
SECURITY INVOKER
AS $$
    SELECT c.chash
      FROM nexus.chunks c
     WHERE c.collection = ANY(p_collections)
       AND (NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m
                         JOIN nexus.catalog_documents d
                           ON d.tenant_id = m.tenant_id AND d.tumbler = m.doc_id
                        WHERE m.tenant_id = c.tenant_id AND m.chash = c.chash
                          AND d.deleted_at IS NOT NULL
                          AND NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m2
                                            JOIN nexus.catalog_documents d2
                                              ON d2.tenant_id = m2.tenant_id AND d2.tumbler = m2.doc_id
                                           WHERE m2.tenant_id = m.tenant_id AND m2.chash = m.chash
                                             AND d2.deleted_at IS NULL)))
       AND (p_where IS NULL OR c.metadata @&gt; p_where)
       AND (p_where_path IS NULL OR c.metadata @@ p_where_path::jsonpath)
       AND (c.chunk_tsv @@ plainto_tsquery('english', p_text) OR p_text &lt;% c.chunk_text)
     LIMIT p_cap
$$;
        </sql>
        <rollback>
            <sql splitStatements="true">
DROP FUNCTION IF EXISTS nexus.text_gate_probe_384(text, text[], jsonb, text, int);
DROP FUNCTION IF EXISTS nexus.text_gate_probe_768(text, text[], jsonb, text, int);
DROP FUNCTION IF EXISTS nexus.text_gate_probe_1024(text, text[], jsonb, text, int);

CREATE FUNCTION nexus.text_gate_probe_384(
    p_text text, p_collections text[], p_where jsonb, p_where_path text, p_cap int
) RETURNS int LANGUAGE sql STABLE SECURITY INVOKER AS $$
    SELECT count(*)::int FROM (SELECT 1 FROM nexus.chunks WHERE false) gate
$$;
CREATE FUNCTION nexus.text_gate_probe_768(
    p_text text, p_collections text[], p_where jsonb, p_where_path text, p_cap int
) RETURNS int LANGUAGE sql STABLE SECURITY INVOKER AS $$
    SELECT count(*)::int FROM (SELECT 1 FROM nexus.chunks WHERE false) gate
$$;
CREATE FUNCTION nexus.text_gate_probe_1024(
    p_text text, p_collections text[], p_where jsonb, p_where_path text, p_cap int
) RETURNS int LANGUAGE sql STABLE SECURITY INVOKER AS $$
    SELECT count(*)::int FROM (SELECT 1 FROM nexus.chunks WHERE false) gate
$$;
            </sql>
        </rollback>
    </changeSet>

    <changeSet id="vectors-012-2" author="nexus-zrcj7">
        <comment>text_gated_search_by_chash_384/768/1024: rank ONLY over the probe's chash set + scope, no text-gate re-evaluation -- restores single-gate-eval, nexus-zrcj7</comment>
        <sql splitStatements="false" stripComments="false">
CREATE OR REPLACE FUNCTION nexus.text_gated_search_by_chash_384(
    p_query        vector(384),
    p_chashes      bytea[],
    p_collections  text[],
    p_where        jsonb,
    p_where_path   text,
    p_n            int
) RETURNS TABLE(id text, content text, collection text, distance float8, metadata jsonb)
LANGUAGE sql
STABLE
SECURITY INVOKER
AS $$
    SELECT encode(c.chash, 'hex'),
           c.chunk_text,
           c.collection,
           (c.embedding_384 &lt;=&gt; p_query)::float8,
           c.metadata
      FROM nexus.chunks c
     WHERE c.collection = ANY(p_collections)
       AND c.embedding_384 IS NOT NULL
       AND c.chash = ANY(p_chashes)
       AND (NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m
                         JOIN nexus.catalog_documents d
                           ON d.tenant_id = m.tenant_id AND d.tumbler = m.doc_id
                        WHERE m.tenant_id = c.tenant_id AND m.chash = c.chash
                          AND d.deleted_at IS NOT NULL
                          AND NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m2
                                            JOIN nexus.catalog_documents d2
                                              ON d2.tenant_id = m2.tenant_id AND d2.tumbler = m2.doc_id
                                           WHERE m2.tenant_id = m.tenant_id AND m2.chash = m.chash
                                             AND d2.deleted_at IS NULL)))
       AND (p_where IS NULL OR c.metadata @&gt; p_where)
       AND (p_where_path IS NULL OR c.metadata @@ p_where_path::jsonpath)
     ORDER BY c.embedding_384 &lt;=&gt; p_query, encode(c.chash, 'hex')
     LIMIT p_n
$$;

CREATE OR REPLACE FUNCTION nexus.text_gated_search_by_chash_768(
    p_query        vector(768),
    p_chashes      bytea[],
    p_collections  text[],
    p_where        jsonb,
    p_where_path   text,
    p_n            int
) RETURNS TABLE(id text, content text, collection text, distance float8, metadata jsonb)
LANGUAGE sql
STABLE
SECURITY INVOKER
AS $$
    SELECT encode(c.chash, 'hex'),
           c.chunk_text,
           c.collection,
           (c.embedding_768 &lt;=&gt; p_query)::float8,
           c.metadata
      FROM nexus.chunks c
     WHERE c.collection = ANY(p_collections)
       AND c.embedding_768 IS NOT NULL
       AND c.chash = ANY(p_chashes)
       AND (NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m
                         JOIN nexus.catalog_documents d
                           ON d.tenant_id = m.tenant_id AND d.tumbler = m.doc_id
                        WHERE m.tenant_id = c.tenant_id AND m.chash = c.chash
                          AND d.deleted_at IS NOT NULL
                          AND NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m2
                                            JOIN nexus.catalog_documents d2
                                              ON d2.tenant_id = m2.tenant_id AND d2.tumbler = m2.doc_id
                                           WHERE m2.tenant_id = m.tenant_id AND m2.chash = m.chash
                                             AND d2.deleted_at IS NULL)))
       AND (p_where IS NULL OR c.metadata @&gt; p_where)
       AND (p_where_path IS NULL OR c.metadata @@ p_where_path::jsonpath)
     ORDER BY c.embedding_768 &lt;=&gt; p_query, encode(c.chash, 'hex')
     LIMIT p_n
$$;

CREATE OR REPLACE FUNCTION nexus.text_gated_search_by_chash_1024(
    p_query        vector(1024),
    p_chashes      bytea[],
    p_collections  text[],
    p_where        jsonb,
    p_where_path   text,
    p_n            int
) RETURNS TABLE(id text, content text, collection text, distance float8, metadata jsonb)
LANGUAGE sql
STABLE
SECURITY INVOKER
AS $$
    SELECT encode(c.chash, 'hex'),
           c.chunk_text,
           c.collection,
           (c.embedding_1024 &lt;=&gt; p_query)::float8,
           c.metadata
      FROM nexus.chunks c
     WHERE c.collection = ANY(p_collections)
       AND c.embedding_1024 IS NOT NULL
       AND c.chash = ANY(p_chashes)
       AND (NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m
                         JOIN nexus.catalog_documents d
                           ON d.tenant_id = m.tenant_id AND d.tumbler = m.doc_id
                        WHERE m.tenant_id = c.tenant_id AND m.chash = c.chash
                          AND d.deleted_at IS NOT NULL
                          AND NOT EXISTS (SELECT 1 FROM nexus.catalog_document_chunks m2
                                            JOIN nexus.catalog_documents d2
                                              ON d2.tenant_id = m2.tenant_id AND d2.tumbler = m2.doc_id
                                           WHERE m2.tenant_id = m.tenant_id AND m2.chash = m.chash
                                             AND d2.deleted_at IS NULL)))
       AND (p_where IS NULL OR c.metadata @&gt; p_where)
       AND (p_where_path IS NULL OR c.metadata @@ p_where_path::jsonpath)
     ORDER BY c.embedding_1024 &lt;=&gt; p_query, encode(c.chash, 'hex')
     LIMIT p_n
$$;
        </sql>
        <rollback>
            <sql splitStatements="true">
DROP FUNCTION IF EXISTS nexus.text_gated_search_by_chash_384(vector, bytea[], text[], jsonb, text, int);
DROP FUNCTION IF EXISTS nexus.text_gated_search_by_chash_768(vector, bytea[], text[], jsonb, text, int);
DROP FUNCTION IF EXISTS nexus.text_gated_search_by_chash_1024(vector, bytea[], text[], jsonb, text, int);
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
